# Valid placeholder markers (no spec/concern/task ID exists yet)
KNOWN_PLACEHOLDERS=(
  # Spec placeholders
  "SPEC-PENDING"      # Spec being written, implementation ahead of docs
  "SPEC-PROTOTYPE"    # Experimental feature, may not need spec
  "SPEC-TBD"          # Uncertain which spec covers this

  # Concern placeholders
  "CONCERN-PENDING"   # Concern identified, not yet documented
  "CONCERN-PROTOTYPE" # Experimental, concern TBD
  "CONCERN-TBD"       # Uncertain concern category

  # Task placeholders
  "TASK-PENDING"      # Task created but not in plan yet
  "TASK-PROTOTYPE"    # Exploratory work
  "TASK-TBD"          # Uncertain task reference

  # Test placeholders
  "TEST-PENDING"      # Test planned but not specified
  "TEST-PROTOTYPE"    # Experimental test
  "TEST-TBD"          # Test coverage uncertain
)

# Spec location for validation
SPEC_DIR="docs/specs"

# Strictness level (warn | error)
VALIDATION_MODE="warn"  # Start lenient
